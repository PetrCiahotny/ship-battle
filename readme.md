curl -i -X POST http://localhost:8000/index.php?route=/user/login -H "Content-Type: application/x-www-form-urlencoded" -d "name=uziv1&password=heslo&action=p%C5%99ihl%C3%A1sit+se"


zapomenuté nastavení State:
Logger::log('STACK: '.print_r(debug_backtrace(), true));
Hledat použítí:
write, read


 

1. Docker
1. Git
1. HTTP
1. Client (JS) vs Server (Php, Asp.net, NodeJS)

Pole v POST formuláři
